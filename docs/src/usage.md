# Usage

`JCGEExamples` provides reference models ported from the literature.

```julia
using JCGEExamples
result = JCGEExamples.StandardCGE.solve()
```

Each model exposes `model`, `baseline`, `solve`, and usually `datadir`.

